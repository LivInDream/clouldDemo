server:
  port: 8001

spring:
  application:
    name: payment-8001
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    data-username: root
    password: root
    url: jdbc:mysql://localhost:3306/study?useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&allowMultiQueries=true

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.springcloud.payment.pojo
